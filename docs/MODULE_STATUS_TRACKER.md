# MODULE_STATUS_TRACKER.md

> A living dashboard of where each Phase 1 module stands across every layer.
> Update this in the same PR that moves a square. A stale tracker is worse than no tracker.

---

## 1. Legend

| Symbol | Meaning |
|---|---|
| ⬜ | Not started |
| 🟦 | In progress |
| 🟨 | Code complete, not yet tested |
| 🟩 | Tested & merged |
| ✅ | Released to production |
| ⛔ | Blocked (see Notes column) |
| — | Not applicable to this module |

Layers tracked:

- **DB** — Flyway migrations defining the module's tables.
- **Backend** — Service, repository, and domain logic in Spring Boot.
- **API** — REST endpoints exposed and documented in [API_STANDARDS.md](./API_STANDARDS.md).
- **Mobile** — Feature implementation in Flutter (data/domain/presentation).
- **Tests** — Unit + integration tests meeting [DEVELOPMENT_RULES.md §7](./DEVELOPMENT_RULES.md#7-testing-expectations) targets.

---

## 2. Status Matrix — Phase 1

| Module | DB | Backend | API | Mobile | Tests | Notes |
|---|---|---|---|---|---|---|
| **auth**                       | 🟩 | 🟩 | 🟩 | 🟨 | 🟩 | V3 migration; register/login/refresh/sessions/password-reset; integration + unit tests. **Mobile (Flutter):** patient app scaffolded under `mobile/` — two-step register (OTP), login, session bootstrap + logout. Riverpod 2.x (manual providers, no codegen) + Dio (snake_case, RS256 bearer, single-flight 401 refresh) + `flutter_secure_storage` token store + go_router auth redirect; UI from design tokens. **Role-aware routing (physio app, C1):** the access token's `role` claim (`shared/auth`: `jwt`/`AccountRole`) is carried in `AuthState` (resolved before authenticating, no wrong-shell flash); the router partitions a physiotherapist into `/physio/*` (a `PhysioShell` with Today/Patients/Availability/Profile — stubs except a working sign-out) and keeps every other account in the patient app. **Backend (C0):** `DevPhysioSeeder` (`@Profile local/dev`) upserts one `ROLE_PHYSIO` login (README §5.2) since there is no physio self-registration; unit-tested. `flutter analyze` clean; widget + network + role-routing/role-decode unit tests green. **Today's schedule (C2) + actionable appointment detail with lifecycle transitions (C3) + the physio discussion side (C4) + treatment-note authoring on COMPLETED visits (C5) + the patient roster & history (C6) + availability management (C7) + Today-schedule activity badges (C8) now land (see the appointments/discussion/treatment_notes/patients/availability rows). Not yet device-tested or merged.** |
| **patients**                   | 🟩 | 🟩 | 🟩 | 🟨 | 🟩 | V4 migration; primary patient auto-created at registration; `PatientAccessPolicy` exposed for other modules. **Mobile (Flutter):** patient app shell — 4-tab bottom nav (Home/Appointments/Family/Profile via `StatefulShellRoute.indexedStack`, per-tab back stacks per UI_UX §8.1) + read-only **Profile** (the primary patient from `GET /patients`: identity, DOB/age, sex, contact, blood group, allergies, plus signed-in devices + sign out) and **Family** list (managed family members with empty state). Home greets by first name and shows an appointments placeholder. Now **interactive**: a reusable `PatientFormScreen` adds a family member (`POST /patients`, relationship required ≠ SELF), edits any patient (`PATCH /patients/{id}` — WYSIWYG: blank clears, null leaves unchanged), and removes a family member (`DELETE`, soft; primary patient can't be removed), reached from a Family "+" action / tappable tiles and a Profile "Edit" action; form routes live outside the shell (`/patients/new`, `/patients/:id/edit`). Shared `AppTextField` gained `readOnly`+`onTap` (tappable date-of-birth picker) and `maxLines`. `PatientSex`/`LocalDateConverter` in `shared/`. An **active Patient context** (F1.7, PATIENT_RELATIONSHIP_MODEL §7) now lives in Riverpod — `selectedPatientIdProvider` (in-memory session selection) resolved by `activePatientProvider` against the live list, so a stale / removed / cross-account id silently falls back to the primary patient. A `PatientSwitcher` at the top of **Home** shows the active patient and, when the account manages more than one, opens a sheet to switch (active one ticked; quick **Add family member** → `/patients/new`); switching refetches Home's upcoming-appointments card (now scoped to the active patient) and pre-selects that patient when booking. The Appointments tab stays an account-wide roster by design. `flutter analyze` clean; patient-model (incl. request DTO serialization) + Family/Form + active-patient-resolver + switcher widget tests green. **Physiotherapist roster + history (C6, F1.16):** the physio app's Patients tab is now the practice roster — `GET /patients` returns every (alive) patient for a `ROLE_PHYSIO` (link fields null), so it reuses `patientsProvider`, name-sorts with a client-side search, and taps into a **read-only** physio patient detail (`/physio/patients/:id`, under `/physio/*`; `extra` carries the `Patient`, with a roster-resolve fallback for deep links): identity + demographics + the clinically relevant fields (blood group, allergies, free-text notes), and a **Treatment history** link. That history reuses `TreatmentNotesTimelineScreen`, now parameterized by a `TreatmentHistoryViewer` (`patient` default / `physio`) so a note tile opens the appointment in the correct app area (`/physio/appointments/:id`, never bounced by the role redirect) and the empty-state copy fits the viewer. `flutter analyze` clean; physio roster (sort/search/empty/no-match) + patient-detail + history-viewer (route-prefix/empty-copy) widget tests green. Device testing deferred. Not merged. |
| **availability**               | 🟩 | 🟩 | 🟩 | 🟨 | 🟩 | V5 migration; rules + blackouts CRUD; `SlotExpansionService` pure function; blackout EXCLUDE-GIST overlap guard. **Mobile (Flutter) — physiotherapist management (C7, F1.8 enabler):** the physio app's Availability tab now manages the two *stored* inputs that compute bookable slots (slots themselves are never stored — CLAUDE.md §11): recurring weekly **working hours** (`/availability/rules`) and one-off **time off** (`/availability/blackouts`). New `availability` feature (api → repository → autoDispose `FutureProvider`s → freezed models). The hub lists active rules (Monday-first; only open-ended rules — archiving via `DELETE` sets `effective_to`, which removes a rule from the list) and time-off windows, each with a confirm-then-remove affordance, plus pull-to-refresh. **Add working hours** form: day-of-week (wire `0=Sun…6=Sat`), start/end time, slot length (15/30/45/60), and an IANA timezone (curated list, default `Asia/Kolkata` — the codebase convention; a full picker is later); start/end alignment on slot boundaries is validated client-side so the physio sees it before the server's 422, and the rule is created open-ended (effective from today). **Add time off** form: start/end instants (sent UTC) + optional reason; an overlapping window surfaces the server's 409 (`availability.blackout_overlap`) message inline. Routes under `/physio/*` (`/physio/availability/rules/new`, `/physio/availability/blackouts/new`) so the role redirect keeps non-physios out; each returns `true` so the hub refetches. `flutter analyze` clean; format (day-label/order/clock/range) + repository wire-formatting (date-only `effective_from`, UTC instants, reason trim/null) + hub (list/hide-archived/empty/remove-confirm) + rule form (save wire values / misaligned-slot blocks save) + blackout form (409 surfaced) widget/unit tests green. Device testing deferred. Not merged. |
| **appointments**               | 🟩 | 🟩 | 🟩 | 🟨 | 🟩 | V6 migration; booking validates via `SlotExpansionService`; state machine + cursor list + reschedule + idempotency; EXCLUDE constraint enforced + asserted. **Mobile (Flutter):** Appointments tab is now interactive — a timeline of upcoming (Requested/Confirmed/In progress) vs past appointments with status chips, pull-to-refresh, and tappable rows. **Book** flow (`POST /appointments` with a generated `Idempotency-Key`): pick patient (primary or a family member) → date → a live slot from `GET /availability` → optional reason, so only real slots are bookable. **Detail** view with the patient's write actions while still open (Requested/Confirmed): **cancel** (`POST /appointments/{id}/transitions` → CANCELLED/PATIENT_CANCELLED) and **reschedule** (`POST /appointments/{id}/reschedule` — same patient/physio kept server-side, so the form is the book form minus the patient picker, with the current date + reason prefilled and live slots; returns a *new* appointment, so the detail replaces itself with the new one's detail). Home's "upcoming" card wired to the next appointment. The shared `SlotPicker` widget is used by both book and reschedule. Booking/detail/reschedule routes live outside the shell (`/appointments/book`, `/appointments/:id`, `/appointments/:id/reschedule`); instants handled UTC→local. Slot models live in the `appointments` feature (booking is the only mobile consumer of availability). The timeline now **paginates**: `appointmentsProvider` is a cursor-paging `AsyncNotifier` (accumulated items + `hasMore`/`isLoadingMore`) that auto-loads the next page near the bottom, with a "Load more" button fallback when the list is too short to scroll. `flutter analyze` clean; appointment-model serialization + list/booking-guard/detail/reschedule-guard + load-more widget tests green. **Physiotherapist schedule (C2, F1.12, read-only):** the physio app's Today tab is now a day schedule — `AppointmentsApi.list` gained `from`/`to` (sent as UTC instants; the backend already filters `scheduledAt >= from && < to`), and `physioScheduleProvider` fetches the local calendar day `[00:00, next 00:00)` over a prev/today/next day stepper (`scheduleDayProvider`). Each row shows time/patient name/status (names resolved from `GET /patients`, which returns the full roster for a physio) and taps into a physio appointment detail at `/physio/appointments/:id` (kept under `/physio/*` so the role redirect excludes non-physios). **Physiotherapist transitions (C3, APPOINTMENT_FLOW §3.1):** that detail is now actionable — it offers only the transitions legal for the current status (REQUESTED → Confirm/Reject; CONFIRMED → Start/No-show/Cancel; IN_PROGRESS → Complete/Cancel; terminal states none) via the generalized `AppointmentsRepository.transition(id, to, reason?, note?)` over `POST /appointments/{id}/transitions` (the patient `cancel()` is now a thin wrapper). Cancellations (Reject/Cancel) require a mandatory note (policy §7) and send `PHYSIO_CANCELLED`, or `OTHER` for the rare in-progress emergency cancel (§3.1); each action confirms, updates the screen in place from the returned appointment, and invalidates the day schedule. The matrix lives in `physio_appointment_actions.dart` (`physioActionsFor`/`physioCancelReasonFor`). The physio detail also links to the discussion thread (C4) and, once COMPLETED, hosts the treatment-note write section (C5) — both detailed in the discussion/treatment_notes rows. `flutter analyze` clean; api `from`/`to`-wiring + schedule list/empty/day-stepper + transition (actions-per-status/confirm/mandatory-note) widget tests green. **Schedule activity badges (C8):** each Today row now surfaces the appointment's discussion state at a glance — an **unread** count badge (patient messages the physio hasn't read) and a **pending-files** count (the files attached to those unread messages — the ones still to open). A new `physioScheduleActivityProvider` (autoDispose) fans the per-thread `unread-count` calls out across the day's *live* appointments (dead states skipped, mirroring the Home roll-up §9); for a thread with unread messages it reads the newest page once and counts attachments on the newest `unreadCount` patient-side messages (exactly the unread set — the read marker only moves forward and the physio's own messages don't count), so the file scan is bounded and only runs where there's unread. A failed call degrades to zero (under-counts, never breaks the list); pull-to-refresh re-runs both. `flutter analyze` clean; activity-provider (aggregate/skip-dead+zero/newest-unread-files-only/physio-excluded/degrade) + tile-badge (shown-with-activity / hidden-without) tests green. **Device testing deferred. Not merged.** |
| **discussion**                 | 🟨 | 🟨 | 🟨 | 🟨 | 🟨 | V7 + V10 migrations; text messages (read markers, 5-min edit/delete window, cursor list, physio-only INSTRUCTION, CANCELLED/NO_SHOW read-only for patient side); V10 adds `discussion_message_attachments` — `ATTACHMENT_ONLY`/`fileIds[]` wired to `file_objects` with per-patient + `AVAILABLE` + ≤10 checks. Body-only paths 🟩; attachment paths code-complete, integration test (`DiscussionAttachmentIntegrationTest`) pending Docker. **Mobile (Flutter):** appointment-scoped discussion thread (F1.14), opened from the appointment detail screen. Chat-style timeline (newest at the bottom) with day separators — physiotherapist messages incoming/left, patient messages outgoing/right, `INSTRUCTION` as an emphasised accent card; cursor "Load earlier messages" pages older history. Composer posts a patient text as `QUESTION` (`POST .../messages`); the sender can edit/delete *their own* text within the 5-min window (long-press → sheet → `PATCH`/`DELETE`), gated client-side on `senderAccountId == sub` (decoded from the access token via the new `shared/auth/currentAccountIdProvider`) so it never offers a doomed action. The read marker advances to the newest message on open and after posting (`POST .../read`). Composer is hidden behind a read-only notice on CANCELLED/NO_SHOW, mirroring `DiscussionAccessPolicy`. Incoming attachments render as read-only chips (name/type/size); the patient can now **attach** files to an outgoing message via the `files` upload flow (F1.15) and **open** any attachment (tap → `GET /files/{id}/download` → presigned URL opened in the external viewer). The **Home "unread" aggregate** (DISCUSSION_SYSTEM_DESIGN §9) now lands: a new `unreadSummaryProvider` fans out the per-thread `unread-count` calls in parallel across the account's *live* appointments (dead states — cancelled/no-show/rescheduled — skipped to bound the fan-out, since Phase 1 has no server-side aggregate endpoint; a failed count counts as 0). Home shows a single "N unread messages" card (hidden when nothing is unread) that opens `/discussions/unread` — an **index of appointments with unread**, not a merged feed (§9/§10). **Physiotherapist side (C4):** the same `DiscussionScreen` now serves both apps via a `DiscussionViewer` (default `patient`, or `physio`) — the thread is identical, only the perspective differs, so it is parameterized rather than forked (unlike the appointment-detail screens, which differ in actions). On the physio side PHYSIO messages are outgoing, the composer posts a `REPLY` or — via an **Instruction** toggle — an `INSTRUCTION` (`postPhysioMessage`; backend derives the PHYSIO sender role and enforces physio-only INSTRUCTION), edit/delete applies to the physio's own messages, and the thread **stays writable on every status** (incl. CANCELLED/NO_SHOW) per `DiscussionAccessPolicy` (physio retains write access; ROLE_PHYSIO short-circuits patient access so attachments work too). Opened from the physio appointment detail (a Discussion action) at `/physio/appointments/:id/discussion` (under `/physio/*`; notification deep-link fallback resolves by id). `flutter analyze` clean; message-serialization + thread (incoming/outgoing/empty/read-only) + unread-summary (aggregate/dead-state-skip/empty) + unread-index (rows/caught-up) + physio-thread (outgoing-side/always-writable/REPLY-vs-INSTRUCTION/empty-copy) widget tests green. Device testing deferred. Not merged. |
| **files**                      | 🟨 | 🟨 | 🟨 | 🟨 | 🟨 | V9 migration; presigned PUT/GET via `FileStorePort` (MinIO adapter, `io.minio` 8.5.14); presign→complete flow with server-side magic-byte + size verification (QUARANTINE on fail); per-mime size caps; daily cap; soft delete blocked while referenced (`FileReferenceGuard` port, implemented by discussion). Storage edge behind a port (tests use in-memory fake). `FileValidationTest` green; integration tests + real MinIO adapter test pending Docker. **Mobile (Flutter):** upload engine (F1.15, increment 1 of 3) — `files` feature data layer: `presign → PUT-direct-to-storage → complete` orchestrated in `FilesRepository.upload` (the size is taken from the actual bytes so it always matches the signed length), behind `FilesApi` (the authenticated app Dio for the API calls + a dedicated **bare** `uploadDioProvider` — no auth interceptor, no baseUrl, no default content type — for the presigned PUT, sending exactly the presigned headers). Freezed models for the presign request/response + `FileView` (`FileKind`/`FileStatus` enums). `flutter analyze` clean; model-serialization + repository-orchestration unit tests green. **Increment 2 (composer pick/attach) landed:** an **Attach** action in the discussion composer (camera / gallery via `image_picker` 1.2.2 BSD-3-Clause, file via `file_picker` 8.1.0 MIT) picks → client-side type/size pre-flight (`uploadTypeForFilename`, mirroring the backend `FileMime` caps) → `FilesRepository.upload` → stages the AVAILABLE file as a removable chip → sends it on the next message (text → `QUESTION` + `file_ids`, no text → `ATTACHMENT_ONLY`). Picker behind a `FilePickerService` seam (fake in tests); composer attach-and-send widget test green. **Increment 3 (download/open) landed:** tapping an attachment chip resolves it via `GET /files/{id}/download` to a short-lived presigned GET URL and opens it externally (`url_launcher` 6.3.0, BSD-3-Clause) — no bytes touch app storage and the URL self-expires; the open call sits behind a `UrlOpener` seam (fake in tests). All three increments code-complete; `DownloadTarget` model-parse + repository-download unit tests and a tap-to-open widget test green. **On-device upload fix (D3):** presigned URLs were minted against `healyn.s3.endpoint` (default `http://localhost:9000`), which a phone/emulator resolves to *itself*, so the direct-to-storage PUT failed even though the presign API call succeeded. Added `healyn.s3.public-endpoint` (`HEALYN_S3_PUBLIC_ENDPOINT`, blank → falls back to `endpoint`): `FilesConfig` now builds a second, `minioPresignClient` against `presignEndpoint()` used **only** for `presignPut`/`presignGet` (SigV4 signs the host, so the URL can't be rewritten after signing — it must be minted against the reachable host), while server-side stat/read/delete keep the internal `@Primary` client. README §5.3 documents the LAN-IP / `10.0.2.2` setup. Mobile: `FilesRepository.upload` now guards the storage PUT separately so a failure reads as a clear upload error (`upload_failed`) instead of a generic transport message. `HealynS3PropertiesTest` (fallback/override) + a PUT-failure repository test green; `flutter analyze` clean (143 mobile tests). Device testing deferred. Not merged. |
| **treatment_notes**            | 🟨 | 🟨 | 🟨 | 🟨 | 🟨 | V8 migration; one note per appointment (UNIQUE `appointment_id`); PUT-upsert gated on `COMPLETED`; physio-only write, patient read; cursor patient-timeline. Integration tests written; run pending Docker (unavailable in authoring session). **Mobile (Flutter):** patient read view (F1.17) — the physiotherapist's note surfaces read-only on the appointment **detail** screen for `COMPLETED` appointments (`GET /appointments/{id}/treatment_note`) as diagnosis / notes / recovery-instructions / next-review blocks, with an empty state until the physio writes one (the backend `treatment_notes.not_found` 404 maps to a null note in the repository) and an inline retry on load failure. Data layer mirrors `discussion` (api → repository → autoDispose family provider → freezed model). The **patient timeline** now lists every note (`GET /patients/{id}/treatment_notes`, cursor-paged newest-first, `TreatmentNotePage` model) on a dedicated screen reached from a **Treatment history** entry on Profile — each card (date + diagnosis/notes snippet + optional next-review line) taps through to its appointment detail; pull-to-refresh + load-on-scroll, with a name subtitle so the same screen serves a family member's history. Date formatting extracted to a shared `treatment_notes_format.dart` (reused by the detail section). **Physiotherapist write side (C5, F1.17):** the physio appointment detail now mounts a write-capable note section on `COMPLETED` — reusing the same `treatmentNoteForAppointmentProvider` and an extracted shared `TreatmentNoteCard` (now used by both the patient read section and the physio side, DRY) — with an **Add / Edit** affordance the patient lacks. The editor (`PhysioTreatmentNoteScreen` at `/physio/appointments/:id/treatment_note`, under `/physio/*`) takes diagnosis / notes / recovery-instructions free text plus an optional next-review date+time and `PUT`s `/appointments/{id}/treatment_note` via the new `TreatmentNotesRepository.upsert` (one endpoint covers first write *and* later edits — the server upserts). The repository trims fields, drops blanks to null (so `include_if_null:false` omits them and empty fields clear server-side), and sends the review instant as UTC; the save button is gated on the same "≥1 of the three text fields" rule the server enforces (mirrors the 422 instead of round-tripping it). On save the appointment's note provider is invalidated so both the physio detail and the patient read view re-read it. `UpsertTreatmentNoteRequest` freezed model added (codegen regenerated). **(C6)** the patient timeline `TreatmentNotesTimelineScreen` is now reused by the physiotherapist's patient history via a `TreatmentHistoryViewer` (patient/physio) controlling the appointment route prefix + empty-state copy — see the patients row. `flutter analyze` clean; model + page-serialization + section (data/empty/error) + timeline (render/empty/paginate) + physio write (save-gating / new-note-upsert / edit-prefill-re-upsert / section empty-vs-existing) + repository trim-null-UTC unit/widget tests green. Device testing deferred. Not merged. |
| **notifications**              | 🟩 | 🟨 | 🟨 | 🟨 | 🟨 | V11 `notification_outbox` (transactional outbox, one row per recipient, payload = IDs only per Hard Rule #4). `NotificationPublisher` writes rows in the caller's tx; wired into booking (REQUESTED/CONFIRMED/CANCELLED), discussion (NEW_MESSAGE), treatment notes (NOTE_ADDED). V13 `fcm_tokens` + `FcmToken`/`FcmTokenService` + `POST /auth/fcm_tokens` (idempotent re-registration keyed on token — read-then-upsert, not race-safe under concurrent same-token POSTs; controller in `notifications/web` to keep `auth` dependency-free). Dispatch runs **claim → send (no tx) → record**: `OutboxTransactions.claimDue` locks due rows SKIP-LOCKED and leases them (`lease-seconds`, default 60) so the FCM call in `OutboxDispatcher` holds no row lock or DB connection; `recordOutcome` finalises each row in its own short tx (SENT / exp-backoff reschedule / DEAD at `maxAttempts`) and retires invalid tokens. A throwing send is isolated per-row — no batch rollback or head-of-line block. `OutboxPoller` (`@Scheduled`, disabled in tests) drives it. Delivery behind `FcmSenderPort`: `FirebaseFcmSender` (firebase-admin) when `HEALYN_FCM_CREDENTIALS_PATH` set, else `LoggingFcmSender` — but only under the `local`/`test` profiles; in any other profile `NotificationsConfig` **fails fast** on blank credentials (mirroring `JwtKeyProvider`) so a misconfigured deploy can't silently drop all push. **Per-account push opt-outs** (API_STANDARDS §9.8): V14 `notification_preferences` (one row per account, four user-facing category booleans, default all-enabled) + `NotificationPreferences`/`NotificationCategory` (kind→category map) + `NotificationPreferencesService` + `GET`/`PATCH /notifications/preferences` (partial PATCH, lazy row create, defaults synthesised on read — no write). Enforcement is at **enqueue**: `NotificationPublisher` skips writing an outbox row for a recipient who opted out of that kind's category, so an opt-out never reaches the outbox. **Mobile (Flutter):** a `notifications` feature — a Notification settings screen reached from **Profile → Settings**, one switch per category over `GET`/`PATCH /notifications/preferences` (api → repository → `AutoDisposeAsyncNotifier`). The account is opted-in by default so every switch starts on; toggling is optimistic (flip → PATCH → reconcile with the returned snapshot, revert + SnackBar on failure). The Profile `_TreatmentHistoryCard` was generalised to a reusable `_NavCard` for the new row. `flutter analyze` clean; model + screen (render / toggle-persists / error-retry) widget tests green. Device testing deferred. Not merged. **Unit tests green** (`NotificationPublisherTest` incl. opt-out gating, `NotificationPreferencesServiceTest`, `FcmTokenServiceTest`, `OutboxDispatcherTest`, `FirebaseFcmSenderTest`, `NotificationsConfigTest`); `NotificationPreferencesIntegrationTest` **run green** (Testcontainers PG+Redis — also validates the V14 migration); other integration tests Docker-gated / not yet run in CI (`FcmTokenIntegrationTest`, `OutboxDispatcherIntegrationTest`). On `feat/auth-module` — **not yet merged to main**. **Remaining**: run full integration suite in CI; device-test the mobile preferences screen. |
| **audit**                      | 🟨 | 🟨 | — | — | 🟨 | V12 `audit.audit_log` (separate schema, append-only; INSERT/SELECT grant role-guarded so it's a no-op in CI). `AuditLogger.record` runs in `REQUIRES_NEW` so it persists from read-only contexts (e.g. file download). Wired write/DOWNLOAD seams: file DOWNLOAD + SOFT_DELETE, discussion CREATE/UPDATE/SOFT_DELETE, treatment-note CREATE/UPDATE, appointment CREATE/UPDATE (+ reschedule). Metadata = IDs only. `AuditLoggerTest` green. **READ-path auditing deferred** to a follow-up (likely a web interceptor) — highest volume, separate pass. API not exposed; service-level only. |
| **compliance**                 | 🟩 | 🟩 | 🟩 | ⬜ | 🟩 | V28 enum values (`account_status.PENDING_DELETION`; `audit_action` += ANONYMIZE/PURGE/CONSENT_GRANT/CONSENT_WITHDRAW) + V29 `legal_documents`/`consent_records`/`account_deletion_requests` (+ seeded **DRAFT** Privacy Policy & Terms). Backend: `LegalDocumentService` (public `GET /legal/{kind}[/{version}]`), `ConsentService` (records the 3 signup consents + family-member authority, version-snapshotted, audited), `AccountDeletionService` (password-confirmed request → `PENDING_DELETION` + revoke sessions → cancellable grace → scheduled `DeletionGracePoller` **anonymize-and-retain**: account credentials/contact tombstoned, patient identity PII redacted, FCM tokens dropped, **clinical rows retained de-identified** per Hard Rule #7; hard-purge config-gated OFF). Consent recorded for `auth`/`patients` via their own ports (`RegistrationConsentRecorder`, `ConsentRecorderPort`) so deps point one way. API: `/legal/**`, `/me/consents`, `/me/deletion-request[/cancel]` + registration `consents` block (all three mandatory via `@AssertTrue`) + `authority_attested` on family-add. **Tests:** `ConsentServiceTest`, `AccountDeletionServiceTest` unit-green; `ComplianceIntegrationTest` (Testcontainers PG+Redis) + `FlywayMigrationTest` (tripwire → V29) green. **Mobile UI deferred to a follow-up PR** (consent gate at signup, legal screens, deletion screen). Real legal copy pending legal review. Not device-tested / not merged. |
| **common (infra)**             | — | 🟩 | — | ⬜ | 🟩 | Error envelope, validation, logging, ID gen, base entity, JWT security in place. |

---

## 3. Cross-Cutting Tasks

| Task | Status | Notes |
|---|---|---|
| CI pipeline (backend test, mobile test, lint) | ⬜ |  |
| Dockerized local dev (PG + Redis + MinIO) | 🟨 | `docker-compose.yml` committed (postgres:16, redis:7, minio + bucket init). Not brought up this session — dev reused an existing external `pc-*` stack on the same ports. |
| Flyway baseline migrations V1–V29 | 🟦 | V1 extensions, V2 enums, V3 auth, V4 patients, V5 availability, V6 appointments, V7 discussion (messages + read markers), V8 treatment_notes, V9 file_objects, V10 discussion_message_attachments, V11 notification_outbox, V12 audit.audit_log, V13 fcm_tokens, V14 notification_preferences, V15–V27 (request-first appointments, IDs/lineage/events, search indexes, addresses, document library, slot-minutes, roster index, physio profile), V28 compliance enum values, V29 compliance schema (legal docs / consent / deletion requests) applied. `FlywayMigrationTest` tripwire pins the latest version (bump with every migration). |
| Design tokens implemented in Flutter | 🟨 | `lib/features/shared/design/` — colors, typography, spacing, radii, motion + `HealynTheme` (now incl. `NavigationBarTheme`) from [UI_UX_GUIDELINES.md §12](./UI_UX_GUIDELINES.md#12-implementation-notes-flutter--riverpod). Shared `SectionCard` widget added. Inter font not yet bundled (system default); golden tests pending. |
| Network layer (Dio + interceptors) | 🟨 | `lib/features/shared/network/` — Dio client + `AuthInterceptor` (RS256 bearer, single-flight 401 refresh) + `ApiException` envelope mapping. Unit-tested. |
| Auth token storage (`flutter_secure_storage`) | 🟨 | `TokenStore` + `DeviceIdentity` over `flutter_secure_storage` (Keychain/Keystore); tokens never in plain storage (CLAUDE.md §11). |
| FCM SDK wired in mobile app | 🟨 | `lib/features/shared/push/` — `firebase_core`/`firebase_messaging` (BSD-3-Clause). `PushService` registers the token via `POST /auth/fcm_tokens` after login + on bootstrap, re-registers `onTokenRefresh`, deletes it on logout, and deep-links a tapped data-only push to the appointment by id (`routeForPush`, IDs only per Hard Rule #4). `FcmMessaging` seam keeps it unit-tested; init is guarded so the app runs push-disabled without native config. **Native Firebase project config (google-services.json / APNs / backend service-account) is a per-environment setup step — see README §5.1.** Not merged. |
| Outbox poller + retry policy | 🟩 | `OutboxPoller` (`@Scheduled` fixed-delay) drives a **claim → send-outside-tx → record** sweep (`OutboxTransactions` + `OutboxDispatcher`): SKIP-LOCKED claim with a visibility lease (`lease-seconds`), per-row error isolation, exp backoff (base 2s ×2^n, max 5 → DEAD). Delivery behind `FcmSenderPort`. Unit-tested; integration test Docker-gated. |
| Real FCM adapter (`firebase-admin`) | 🟩 | `FirebaseFcmSender` (data-only messages, IDs only per Hard Rule #4; FCM error→outcome mapping unit-tested). Dependency `com.google.firebase:firebase-admin:9.4.3` (Apache 2.0) added. Activates when `HEALYN_FCM_CREDENTIALS_PATH` is set; otherwise `LoggingFcmSender` is the fallback (`@ConditionalOnMissingBean` + blank-path check) — but only in the `local`/`test` profiles, since `NotificationsConfig` fails fast on blank credentials in any other profile. Credentials provisioned in dev env. |
| OWASP dependency-check integrated | ⬜ |  |
| ADR set written (5 starting ADRs) | ⬜ |  |
| Onboarding `README.md` setup verified by a new dev | ⬜ |  |

---

## 4. Phase 1 Release Readiness Checklist

Phase 1 ships when **every box in §2 is at least 🟩** for the layers it applies to, **and** every cross-cutting task in §3 is 🟩 or ✅, **and**:

- [ ] All P0 features in [FEATURE_ROADMAP.md §2.1](./FEATURE_ROADMAP.md#21-phase-1-feature-inventory) are 🟩 or ✅.
- [ ] Security review per [SECURITY_GUIDELINES.md §14](./SECURITY_GUIDELINES.md#14-security-review-checklist-per-pr) passed on the release branch.
- [ ] Smoke test: a new account can register → add a family patient → book → message + attach → physio confirms → physio completes + adds note — end-to-end on staging.
- [ ] p95 API latency under 250 ms on staging under simulated 30 concurrent users.
- [ ] Backup + restore drill executed successfully on staging.
- [ ] Runbook for incident response written.

---

## 5. How to Update This File

1. Move squares in the same PR that ships the work. Don't batch.
2. If a square moves to ⛔, fill the Notes column with the blocker and link to the issue.
3. Don't invent new modules here without adding them to [SYSTEM_ARCHITECTURE.md §3](./SYSTEM_ARCHITECTURE.md#3-module-breakdown) first.
4. When a module reaches ✅ in production, leave the row and add it to a "Released" section once §2 is full.

---

## 6. Related Documents

- [FEATURE_ROADMAP.md](./FEATURE_ROADMAP.md) — feature-level scope
- [SYSTEM_ARCHITECTURE.md](./SYSTEM_ARCHITECTURE.md) — module breakdown
- [DEVELOPMENT_RULES.md](./DEVELOPMENT_RULES.md) — what "done" means
